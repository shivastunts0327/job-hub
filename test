node{
    
    stage('permissions for Directories'){
    sh label: '', script: '''cd /opt
    sudo git@github.com:shivastunts0327/job-hub.git
    withCredentials([sshUserPrivateKey(credentialsId: 'githubpwd1', keyFileVariable: 'git hub', passphraseVariable: 'git hub', usernameVariable: 'shivastunts0327')]) {
    // some block
}
    sudo apt-get update'''
 }
}
