node{
    
   stage('Push Docker Image'){
    withCredentials([string(credentialsId: 'dockerpwd1', variable: '')]) {
    

}
        sh "docker login -u shivastunts -p ${dockerpwd1}"
     }
     
     }
