node{
    
    stage('Push Docker Image'){
     withCredentials([usernamePassword(credentialsId: 'dockerpwd', passwordVariable: '', usernameVariable: '')]) {
        sh "docker login -u kammana -p ${dockerHubPwd}"
     }
 }
}
